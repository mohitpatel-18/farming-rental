
{
  "name": "farming-rental-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "npx serve -s frontend/build -l $PORT"
  },
  "dependencies": {
    "serve": "^14.2.5"
  }
}
